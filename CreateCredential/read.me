This script creates an encrypted password file that stores username and password.  It can also be used to store any keyvalue pairs that extend past the windows login and password lengths.  Specificly this was created to store AWS Access keys with the corosponding Secret key.