This script creates an encrypted password file that stores username and password, that extends past the windows login and password lengths.  Specificly this was created to store AWS Access and Secret keys.